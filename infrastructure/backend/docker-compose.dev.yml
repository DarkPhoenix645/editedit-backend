services:
  backend:
    # Run with file watching in the mounted code directory.
    command: sh -c "uv run uvicorn app.main:app --host ${HOST:-0.0.0.0} --port ${PORT:-8000} --reload --reload-dir /app"
